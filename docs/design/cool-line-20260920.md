# Cool line art / Instagram thumbnails — 2026-09-20

地域の事業者がAIで仕事を整える姿を、滑らかな墨色の線と淡いセージの面で表現。既存の文章・コース構成・操作は維持し、今回制作した13画像をすべて異なる構図で更新した。

- 生成: built-in image_gen。プロンプトは `cool-line-20260920-prompts.json`。
- 原本: `cool-line-originals/`。公開WebP: `site/static/design-system/studio/images/soft-*.webp`（既存パスを維持）。
- 確認用一覧: `cool-line-contact-sheet.png`。
- Instagram: `@tatsumi.yui` の既存6投稿を認証不要の公式埋め込みページで照合し、各 `EmbeddedMediaImage` の画像を取得。原本は `instagram-originals/`、公開は `site/static/instagram/`。
- 画像のみをPC128px、スマホ104pxで横並びに表示。見出し・説明・投稿フレーム・操作ボタンは除去。画像クリックで元の投稿を新しいタブに開く。スワイプ、Tab、左右キーに対応。
- Instagram原稿の画像内文字は元投稿の一部なので保持。画像を新規投稿へ自動更新する機能は追加しない。
- 公開スナップショットの既存500資産のうち16件のみ変更、6画像追加。残り484資産・runtime15件をバイト単位で保持。
- ビルド: `scripts/build_cool_line_release.py --baseline <直前リリース> --output <新規出力>`。
- 本番のコミット、PR、Cloudflare版、画面/API検証はリリース内production-summary.jsonに記録。

伝える技術レビュー: 忙しい地域の事業者に、AIで仕事を整える身近な使い道を伝える。主導線は既存の講習・相談。Instagramは小さな実践への入口として主導線を妨げない。

検証済み: 1365×900と390×844のアプリ内ブラウザでトップ・Instagramを目視。6画像読込、スマホ104px、文書の横はみ出しなし、画像列の左右キー操作、メニュー開閉とコントラストを確認。PC自己紹介画像も確認。Instagram後のニュース欄の負の余白を局所的に解除した。
