# Everyday AI / cinematic cyber

2026-09-23 request: semi-realistic animated imagery, tasteful Japanese subculture and restrained cyber accents; reduce explanatory text and move compact work examples directly below both diagnoses.

- 18 distinct images, generated with the built-in image_gen tool. Original PNGs: `cyber-originals/`; full prompt set: `cyber-20260923-prompts.json`; overview: `cyber-20260923-contact-sheet.png`.
- Published WebP assets: `site/static/design-system/studio/images/soft-*.webp` (11 active scenes) and `lesson-*.webp` (7 covers), 1536×1024, quality 84. Images use a dated URL to avoid showing old cached artwork.
- Pearl/ink/lavender/mint palette; believable people, PCs and work outputs. The user's approved watercolor portrait, venue photo, Instagram media, portfolio screenshots and article illustrations stay intact.
- Shared CSS continues to style public, speaker and admin pages. Glass overlaps retain readable opaque text surfaces and reduced-motion behavior.
- Homepage teaser paragraphs and portfolio descriptions are removed from HTML through the shared decoration path. Prices, links, forms and expandable course conditions are preserved.
- The three work tabs retain manual/AI modes and keyboard navigation. The example shows only delegated work and human checks, directly after the diagnosis pair.

## Verification and release

`node --test tests/soft-playground.test.mjs tests/studio-glass-motion.test.mjs` and Python tests `test_compact_home`, `test_art_direction`, `test_instagram_images`.

`build_cyber_release.py` verifies the immutable September 22 public manifest and the previous runtime manifest before replacing only owned presentation assets. It checks unchanged prices/forms/link destinations, section order, idempotence and file allowlists. News remains September 22; no article generation or service/API changes are included.

Preview: 1365×900 and 390×844, no horizontal overflow. Work example ~302px desktop / 479px mobile; diagnosis cards ~192px mobile. Public mobile menu, three tabs and native mode radios checked. User-authorized in-app browser, public pages without service login.

Runtime is dry-run bundled, then deployed only from the registered source after main integration and the central Cloudflare guard. The per-task delivery record stores actual PR, SHA and production verification evidence.
