# 公式ページへの経歴追加

- 公式URLのHTMLを2026-09-14に読み取り、現在の公開デザインを基準として編集。
- トップページは講師ボタンだけを「講師のプロフィールを見る」→ `/speaker.html#career` に変更。実績の横スライド、写真、ニュース、CSS、JavaScriptを保持。
- 講師紹介ページは実績カードの重複掲載を廃止し、短い4項目の経歴を追加。既存の写真、デザイン、講習の考え方、問い合わせ導線は保持。
- 正文は `content/speaker.md`。`scripts/apply-speaker-career.py` は現行公式ページのHTMLを基準にこの限定変更を適用する補助スクリプト。
- ビルダー側でも実績カードを出さず、プロフィール・経歴を案内するよう更新。
- 既存テスト2件成功。公式HTMLに対してトップ変更がリンク1箇所のみであること、2ページともCSSとJavaScriptが完全一致することを検証。
- 公開予定HTML SHA256: index `6f8b8fcdc8495fa132816b40b79d841982569a84f6480e4399d99ea1ce86528e`、speaker `590187f79e6b71ecb93b4105d6b2e815f74c54d0265744bc23b3591a9f782b2e`。

## 公開の残件

台帳は `C:/Project/AI相談/cloudflare-runtime` からの公開に限定している。同ディレクトリの既存未コミット変更とHEAD不一致は解消されておらず、本作業の分離worktreeからのデプロイも現行ルールで禁止されている。今回のmain統合だけでこの条件は解消しない。

実施可能な次の手順は、既存変更をそれぞれの所有作業で統合して正本をクリーンにするか、ユーザーが公開元の運用変更を明示すること。その後、全公開アセットとWorkerの現行版照合、ガード、デプロイ、本番確認を行う。別作業のデータを上書き・一括コミットして条件を満たさない。
