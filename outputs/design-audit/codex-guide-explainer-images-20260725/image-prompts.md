# Codex guide explanatory image prompts

Generated with the built-in `image_gen` tool on 2026-07-25.

## Roles

Four connected columns explaining `Worktree = 作業を分ける`, `Commit = 変更を保存`, `Pull Request = 公開前に確認`, and `Deploy = 本番へ反映`. Clean off-white instructional infographic, large Japanese text, navy/blue/mint palette, no decorative characters.

## Five-step flow

Five numbered steps: `作業を分ける / Worktree`, `サイトを直す / Local / Worktree`, `変更を残す / Commit`, `内容を確認 / Pull Request`, and `本番へ出す / Deploy`. Clean instructional infographic with arrows and large text.

## Local and Worktree

Two-column decision chart. Local: `今の作業場所`, `小さな修正`, `ほかの作業がない`. Worktree: `別の作業場所`, `並行して進める`, `今の変更を守る`. Bottom rule: `混ざりそうなら Worktree`.

## Release states

Three connected states: `ローカル / 自分のPCだけ`, `プレビュー / 関係者が確認`, `本番 / 利用者に公開`. Bottom completion rule: `本番URLを開いて完了`.
